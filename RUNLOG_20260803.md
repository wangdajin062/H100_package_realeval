# RUNLOG 2026-08-03 — RunPod H100 重跑实验结果（P0/P1）

> Pod `mhypfkvge474n8` · H100 80GB · venv torch 2.8 · 代码 `ccc7c56`→`819626d`（含路径 B 多 seed + CoT 修复）
> 依据：`RunPod_rerun_execution.md` 阶段 1–2（P0/P1）· 全实验多 seed=5（`config/runpod_h100.yaml`）

## 概览

| 实验 | 结果 | 清单对照 |
|---|---|---|
| **exp2** | MSE 0.7667（std 0.0265）> CE 0.7667 > KL 系 0.5577（std 0.1217） | Tab.5 P0：「Pure-KL 最优」**反转确认** |
| **exp9** | with_cot f1=0.035 fpr=0.032；without_cot f1=0.2892 fpr=0.6239 | Tab.CoT P0：**CoT 无效**（F1 崩塌） |
| **exp1** | acc=0.7863，**std=0.1633**，kl_final=0.3248，drift_final=0.0 | Tab.3 QAD 重跑 |
| **exp3** | drift 随 OVF：no_reg 61.479 → quarter 48.186 → half 35.561 → full **0.0** | Fig.7a P0：**drift 响应验证通过** |
| **exp11** | fp16 0.5328 / int8 0.5441 / **int4 0.6185** / nf4 0.3085（model_source=exp1_qad） | Tab.3 PTQ / Fig.5a：量化分支修复生效 |
| **exp14** | bf16 0.5853 vs **q4km 0.7025**（latency 14.39ms） | Tab.3 Q4_K_M |

## 详细结果

### exp2 — 损失消融（5 变体 × 5 seed，真实 std）
| 变体 | f1 | std | kl_final |
|---|---|---|---|
| kl_only | 0.5577 | 0.1217 | 0.34629 |
| mse_only | **0.7667** | 0.0265 | 3.34172 |
| ce_only | 0.7667 | 0.0265 | 3.34172 |
| kl_mse_combined | 0.5577 | 0.1217 | 0.34629 |
| kl_task | 0.5577 | 0.1217 | 0.34629 |

⚠️ 学生=教师同架构（Qwen2.5-0.5B），蒸馏中 `mse_loss≈0` → `kl_mse≈kl`、`mse≈ce`，loss 区分度受限（论文需说明）。

### exp9 — CoT 消融（CoT 修复后）
| 路径 | f1 | fpr |
|---|---|---|
| with_cot | 0.035 | **0.032**（原 0.9849） |
| without_cot | 0.2892 | 0.6239 |

- FPR 修复生效（0.98→0.03，tie 判 normal + max_new_tokens 64）
- 但 F1 崩塌 0.035 → **CoT 生成式判定不可用**，按清单 Tab.CoT 需**改写/删除「CoT 有效」结论**

### exp1 — QAD 生产蒸馏（5 seed）
- accuracy 0.7863 · **std 0.1633** · kl_final 0.324822 · drift_pct_final 0.0 · snr 3.4–4.6 dB · ovf_activation_step 1400

### exp3 — OV-Freeze 控制（14 点 × 5 seed）
| condition | f1 | f1_std | variance_drift_pct |
|---|---|---|---|
| no_reg | 0.5577 | 0.1217 | 61.479 |
| ov_freeze_quarter | 0.5577 | 0.1217 | 48.186 |
| ov_freeze_half | 0.5577 | 0.1217 | 35.561 |
| ov_freeze_full | 0.5577 | 0.1217 | **0.0** |

**drift 随 OVF 强度递减**（不再恒 61.479）→ Fig.7a 机理验证通过。

### exp11 — 量化方案（4 方案 × 5 seed，model_source=exp1_qad）
| 方案 | f1 | f1_std | acc |
|---|---|---|---|
| fp16 | 0.5328 | 0.0 | 0.8575 |
| int8 | 0.5441 | 0.0 | 0.8644 |
| **int4** | **0.6185** | 0.0 | 0.8466 |
| nf4 | 0.3085 | 0.0 | 0.8263 |

4 方案完全独立（量化分支修复生效），int4 最优。

### exp14 — BF16 vs Q4_K_M（各 5 seed）
| 运行时 | f1 | f1_std | latency |
|---|---|---|---|
| bf16 transformers | 0.5853 | 0.0 | — |
| **q4km llama.cpp** | **0.7025** | 0.0 | 14.39ms |

## 本轮代码修复（已推送）
- `ccc7c56` 路径 B：exp1/3/11/14 多 seed 支持（真实 std）
- `819626d` exp9 CoT 生成式解析：max_new_tokens 64 + tie 判 normal（FPR 0.98→0.03）

## 完整结果文件位置
Pod `/workspace/H100_package_realeval/outputs/results/`（exp1/2/3/9/11/14 + all_experiments.json，共 6 实验）。

---

## 追加：`scripts/export_to_gguf.py` 修复（手机端 GGUF 导出链路）

### 问题
原脚本把 `outputs/models/exp1_qad`（**只有 PEFT LoRA adapter**：adapter_model.safetensors / head.pt，无 config.json / 无基础权重）直接交给 `llama_cpp.convert`：

1. **缺 LoRA 合并** —— 领域蒸馏能力（LoRA）永远进不了 GGUF；
2. **目录不完整** —— llama.cpp 无法转换 adapter-only 目录。

同时 exp14 的 `student_gguf: "Qwen/Qwen2.5-0.5B-Instruct-GGUF"`（config/experiments.yaml:19）用的是**官方预量化 GGUF**，即手机端链路实际测的仍是通用模型、不含领域蒸馏 —— 与「手机端部署领域蒸馏模型」的论文表述不符。

### 修复（commit 见下）
`scripts/export_to_gguf.py` 重写为 4 步：

1. 基础模型解析：CLI `--base-model` > `adapter_config.json` 的 `base_model_name_or_path`（RunPod 本地 `/workspace/models/Qwen/Qwen2.5-0.5B-Instruct`）；
2. `PeftModel.from_pretrained(base, adapter)` + `merge_and_unload()` —— 领域 LoRA 真正合并；
3. 保存完整 transformers 目录（含 config.json / 合并权重 / tokenizer）到临时目录；
4. GGUF 转换三层回退：`llama_cpp.convert` Python API → `python -m llama_cpp.convert` CLI → **llama.cpp `convert_hf_to_gguf.py`**（`/workspace/llama.cpp`，`--outtype q4_k_m`）。

> 注：GGUF 手机端路径是**生成式判定**（llama.cpp 提示模型输出 0/1，见 `realeval/gguf_backend.py`），`head.pt` 分类头不导出。

### 实测

- 第一次导出（仅 llama_cpp_python 0.3.34，无 convert 模块）：**LoRA 合并成功**（model shards 100% 写出），转换失败 `No module named llama_cpp.convert`；
- 已 clone llama.cpp（`/workspace/llama.cpp`） + 装 gguf 依赖；
- 重跑导出进行中（`outputs/logs/export_gguf2.log`），产出 `outputs/models/exp1_qad_q4km.gguf`（预计 ~240MB）。
- **待办**：导出后用 `gguf_backend` 回测新 GGUF 的 F1，对比官方 GGUF 0.7025 验证领域 LoRA 是否提升手机端模型；若显著提升，更新 exp14 / README 手机端流程。
