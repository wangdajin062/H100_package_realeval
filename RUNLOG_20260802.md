# RunPod 运行日志整理 — 2026-08-02

> **Pod 状态警告**：SSH 探测连续返回 **`container not found`**（2026-08-02 10:1x），
> Pod `mhypfkvge474n8` 当前**处于停止状态**，远程 `/workspace` 日志本次未能拉取。
> 需在 RunPod 网页控制台启动 Pod 后补拉（历史同况见 [[runpod-deployment]] 08-01 记录）。

---

## 1. 今日工作概览（QAD-MultiGuard RealEval）

今日（截至 10:20）为**本地代码修复**为主，远程 Pod 未运行、无新实验产出。

### 1.1 已完成：paper_data 误差条对齐真实字段（commit `ba0ae1d`）

`docs/figure_scripts/paper_data.py`（+16 / -4）：
- 新增 4 个误差条解析项，用 `_from_result` 从实验 JSON 解析 `std` 并纳入 `_MISSING_PLACEHOLDERS` 追踪：

  | 占位符 | 来源实验 | 回退值（论文估算） |
  |---|---|---|
  | `PH_EXP1_ERR` | exp1 `std` | 0.007 |
  | `PH_EXP3_OVF_FULL_ERR` | exp3 `conditions.ov_freeze_full.std` | 0.006 |
  | `PH_EXP11_INT4_ERR` | exp11 `schemes.int4.std` | 0.014 |
  | `PH_EXP14_Q4KM_ERR` | exp14 `models.q4km_0.5b_llama_cpp.std` | 0.007 |

- `QAT_QAD_OVF` 表格 4 行的 `f1_err` 从硬编码改为引用上述解析值。
- **边界**：当前全部实验均为单次运行、无实测 std → 回退论文误差条，并在 placeholder 中标记 **NOT measured**（遵循"引用基线保持不动"原则，见 paper-experiment-fix）。

### 1.3 字段对齐核对 + exp8 P99 错位修复（新增）

新增自动核对工具 **`docs/figure_scripts/check_alignment.py`**（只读不改）：
- AST 解析 `paper_data.py` 全部 65 处 `_from_result(...)` 字段路径，逐一验证能从 `outputs/results/*.json` 解析；
- 校验 6 个 fig 脚本（fig3–fig8）从 paper_data import 的变量均有定义；
- **结构错位检测**：路径中途遇标量（如把扁平 `latencies.int4` 当嵌套路径读）会报 `[MISALIGN]`；
- 退出码 0=对齐 / 1=存在问题；用法 `python docs/figure_scripts/check_alignment.py`。

修复 `paper_data.py` 的 **exp8 时延字段错位**（数据桥接脚本，未动任何 fig*.py）：
- **根因**：`LATENCY_P99_MS` 原读 `latencies.<scheme>`（扁平 dict，仅含 p50 标量），**P99 实际读到的是 P50 值**。
- **修复**：改读权威结构 `latency_detail.<scheme>.p99_ms`（P50 同步改 `latency_detail.<scheme>.p50_ms`）。
- **验证**：`LATENCY_P99_MS` 由错值 `[46.47, 34.299, 28.322]` 修正为 **`[47.082, 37.924, 29.568]`**；self-check 全通过；`generate_all.py` 图 1–7 生成成功。

**核对结论**（check_alignment.py 通过，仅剩 4 处 MISSING 均为单次运行无 std 的正常 fallback）：
| 项 | 状态 |
|---|---|
| 65 处 `_from_result` 字段路径 | ✅ 全部可解析（含 teacher_1.5b / rho_0.0 / q4km_0.5b 含点 key） |
| fig3–fig8 import 变量定义 | ✅ 全部对齐 |
| exp8 P99 | ✅ 已修复 |
| exp1/exp3/exp11/exp14 `std` | ⚠️ 单次运行无实测 std → fallback 论文值（多 seed 重跑后自动生效） |
| exp2 `std` 恒 0.007 | ⚠️ 硬编码占位（checklist 已知，需多 seed 重跑产生实测） |
| exp3 drift 恒 61.479% | ⚠️ OVF 机理未复现（checklist 已知，Pod 重跑） |

> ⚠️ **D 盘空间告警**：D: 9.7G 仅剩 ~817M（92%）。`docs/figure/` 的 **tiff 异常大**（fig4≈128MB / fig5≈111MB / **fig6≈718MB**，dpi=400）且被 git 跟踪；每次 `generate_all.py` 重生成会撑满磁盘（本次 fig8 tiff 因此未生成，已 `git checkout` 恢复原状）。需决策：调低 dpi（需改 fig 脚本，违反约束）或从版本库移除超大 tiff。

### 1.2 核实：昨日待办 exp1_qad 路径隐患已落地

昨日 RUNLOG 遗留待办「修复 `exp1_qad` 多实验共用保存路径」**确认已完成**：
- `realeval/real_backend.py:138` `real_qad_distill_train` 新增 `save_name` 参数，`:399-400` 仅当非 None 时保存到 `outputs/models/<save_name>`。
- 调用方核查：仅 `experiments/exp1_qad_production.py:41` 传 `save_name="exp1_qad"`；exp2 / exp3 / exp10 均默认 `None` 不保存 → **不再互相覆盖**。

## 2. 远程 Pod 状态

- 探测：`runpod-exec.sh` 两次返回 `container not found`，SSH 会话无法建立，日志/git 状态无法同步。
- 待办：RunPod 控制台启动 Pod 后，补拉 `/workspace/H100_package_realeval/outputs/results/` 与 RUNLOG 差异。

## 3. 待办（依赖 Pod 恢复，来自 rerun_checklist.md P0）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 🔴 P0 | **Tab.3 主结果（TAF-28k）全表重跑** | 论文 0.923 更像融合结果，需真实 QAD 文本蒸馏数字 |
| 🔴 P0 | **Tab.CoT 思维链消融** | 结论反转：真实 exp9 CoT 0.23 < 0.29（FPR 0.9573 已修生成式判定） |
| 🔴 P0 | **Tab.4 跨数据集 & 对抗鲁棒全表重跑** | exp5/exp6 已标注 NOT MEASURED |
| 🔴 P0 | **Tab.5 损失函数消融** | 结论反转：真实 exp2 kl_only 最差 0.3875、MSE 最佳 0.7911 |
| 🔴 P0 | **Fig.4/5/6/7** | 训练收敛/SNR、补充消融、损失与教师规模、OVF 配置与激活窗 |
| 🟡 P2 | **exp11 fp16 < int4 诊断** | 本地无明确 bug，`diag_fp16.py`（bf16/fp16/int4/int8/nf4 对照）已上传待跑 |
| 🟡 P2 | **BF16 全管线基线** | 真实无此实验，需重跑产生真实数字 |
| 🟡 P2 | **exp2 多 seed / exp9 CoT / exp3 drift 修复后重跑验证** | 修复已在本地，需 Pod 验证 |

> 若 P0 重跑后结论仍反转：**先改论文结论再改数字**（rerun_checklist §2 原则）。

## 4. 关键路径备忘（不变）

| 资源 | 路径 |
|------|------|
| 代码 | `/workspace/H100_package_realeval`（本地主副本 `D:\Projects\H100_package_realeval`） |
| 数据 | `/workspace/data` |
| 模型 | `/workspace/models` |
| HF 缓存 | `/workspace/hf_cache` |
| 持久 venv | `/workspace/venv` |
| LoRA 适配器 | `/workspace/outputs/lora_manual/best`（best F1 0.6441） |
| 清单 | `C:\Users\wangd\Downloads\rerun_checklist.md` |

## 5. Git 状态

- 最新提交 `ba0ae1d`（2026-08-02 10:19）：paper_data QAT/QAD 误差条对齐（见 §1.1），已推至远程 `wangdajin062/H100_package_realeval`。
- 未提交改动（本次核对/修复）：
  - 修改 `docs/figure_scripts/paper_data.py`：exp8 P99 字段错位修复（见 §1.3）
  - 新增 `docs/figure_scripts/check_alignment.py`：字段对齐核对工具（见 §1.3）
  - `docs/figure/` 已 `git checkout` 恢复原状（验证触发的重生成已回退，避免超大 tiff 撑满磁盘）
- **未修改任何 fig*.py 图像脚本**。

相关：[[runpod-deployment]] [[paper-experiment-fix]]
